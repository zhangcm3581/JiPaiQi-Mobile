# 轮次同步与面板时序 · 0.4.20

2026-09-11。当前上传协议见 [一次提交与恢复](atomic-submit-0.4.20.md)，覆盖历史版本先绑定再传牌的规则。

连接先取得服务端当前版本；本机在可靠新开局第一帧冻结目标版本，连续两次确认同样的完整13张后直接发送带 `start_event_id` 的 `hand.submit`，不发送 `round.join`，不等待绑定确认。返回参与记录与保存手牌在服务端一个事务内完成。下一局可使用第一帧时已知的较新版本，支持缺席数轮再回来；已识别的旧手牌不能随版本变更改标重传。

## 本机阶段

| 游戏画面 | 结果面板 | 协议动作 |
| --- | --- | --- |
| 开局，一键配连续两帧 | 显示空等待网格 | 有可靠入局证据且稳定13张，直接 submit |
| 摆牌中：底部13张/9张/0张，按钮仍在 | 保持显示 | 不重复 submit |
| 全部摆好、尚未确定，按钮仍在 | 保持显示 | 不结束 |
| 确定后，按钮持续消失600ms，等待别人/比牌 | 隐藏 | 不发 end，不解除本局锁定 |
| 同局重新摆牌，按钮连续两帧恢复 | 恢复仍有效的本轮面板 | 不重复上传 |
| 继续或返回大厅任一连续两帧命中 | 隐藏并清空 | 本局一次 end |
| 大厅/局间 | 隐藏，脚本继续 | 等玩家进入下一桌及可靠新开局 |
| 服务器提前换轮/关闭，自己还在摆牌 | 显示空的等待/状态面板，旧牌清空 | 不把旧牌改成新版本 |
| 隐藏时迟到服务器结果 | 继续隐藏 | 仅接受仍有效的本轮结果 |
| 暂停、采集异常、转屏或超过1.5秒无新帧 | 隐藏、打断视觉证据 | 不伪造结束或开局 |

面板固定左上角 `(2,2)`，不拖动、不自动移动；点击可隐藏，同阶段的结果刷新不强行打开。结果数据必须来自服务器且恰好13张，等待网格为空不算不完整结果。

## 素材和确认参数

- 手牌：0.80，白黄底预处理同时应用于模板及 ROI；详见 [hand-background-preprocess.md](hand-background-preprocess.md)。
- 状态图：彩色0.9，开局主要使用“一键配”，另以固定位置的“两对”灰/绿按钮任一命中补充摆牌状态，避免鼠标遮挡一键配时面板闪烁；所有状态图仍为彩色0.9，颜色特征可不配置。
- 结束多个任一命中；继续搜索原840×474画面的 `[0,380,840,94]`，可覆盖中间及右移按钮；返回大厅 `[270,382,153,76]`。
- 常规采样目标200ms，开局待收齐手牌阶段加速至80ms；开局/结束连续2次，离开摆牌与局间确认600ms。实际延迟包含采样、匹配和绘制。

验收涵盖持续摆牌不重发、摆完等待确定仍显示、比牌隐藏、迟到结果不弹出、其他设备先结束、延迟 ack、断线同版本恢复与旧牌拒绝改版本、返回大厅后同一服务继续下一桌。见 [审核记录](full-flow-review-0.4.7.md)。


## 2026-09-10 连续确认补充

即使结束特征仅命中一帧、尚不足以结束本局，也必须中断尚未完成的手牌连续确认。有效13张→单帧结束候选→有效13张，不能被算作连续两帧；已经确认且等待网络确认的请求继续按原 request_id 重试。协议字段与版本不变。手牌还需执行 [数字/花色独立验证](hand-corner-matching-v1.md)。


## 状态模板补充与内置包升级

素材包1.3.0保留“两对”灰/绿两张状态图，ROI为原840×474画面的 `[67,399,82,45]`。原一键配与结束区域不变；结束优先于开局。灰/绿均表示按钮存在，不推断是否有两对。

未经修改的内置1.1.0/1.2.0包可自动升级1.3.0，保留两个ID；用户修改过的包不覆盖。白黄底统一预处理与数字/花色独立验证继续生效。

## 0.4.21 暂停规则

客户端未启动或暂停时不连接服务器，不发业务消息、心跳或重试。暂停关闭连接并清空隐藏面板；保存不同ID也进入暂停，保存相同ID保持原运行状态。点击启动才重新连接、获取当前轮次，并按持久记录恢复与新画面确认流程继续；旧手牌不会因重新启动自动补传。已在暂停前发送出去的数据无法撤回，服务端已收记录保留。网络重连只在启动工作期间运行。
